var TempService = (function() {
    function calcule(number) {
        var result = 1;
        if(number === 0) return result;
        return 120;
    }
    return {
        "calcule": calcule
    }
    })();